# === Anthropic Configuration (default provider) ===
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# === Databricks Configuration ===
# Recommended: Use `databricks configure --token` to set up ~/.databrickscfg
# Alternatively, set these environment variables:
DATABRICKS_HOST=https://your-workspace.databricks.com
DATABRICKS_TOKEN=your-databricks-token-here

# Or use Service Principal (for production):
# DATABRICKS_CLIENT_ID=your-client-id
# DATABRICKS_CLIENT_SECRET=your-client-secret

# === OpenAI Configuration ===
OPENAI_API_KEY=your-openai-api-key-here

# === Azure OpenAI Configuration ===
AZURE_OPENAI_API_KEY=your-azure-api-key-here
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com

# === Agent Configuration (optional) ===
# Override defaults via environment variables
AGENT_PROVIDER=anthropic                           # anthropic, databricks, openai, azure
AGENT_MODEL=claude-sonnet-4-5-20250929            # Provider-specific model name
AGENT_WORKSPACE=./workspace                        # Workspace directory
AGENT_AUTO_APPROVE=false                           # Auto-approve agent actions
AGENT_ENDPOINT=                                    # Custom endpoint (Databricks, Azure)
